# Proto‑Vocab Starter (Step 1)

Clean base for the project before handing to Claude.

Includes:
- Router + pages (Home, Dictionary, Simulator, Studio, Admin)
- Tailwind setup

Usage:
1) `npm create vite@latest proto-vocab-10nov -- --template react-ts`
2) `cd proto-vocab-10nov && npm i`
3) `npm i -D tailwindcss postcss autoprefixer && npx tailwindcss init -p`
4) Drop these files (overwrite existing ones)
5) `npm i react-router-dom`
6) `npm run dev`
